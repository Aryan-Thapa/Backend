<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-zinc-800 h-screen text-white">
    <div>
      <h1 class="p-4 text-xl font-bold text-gray-400">All Users</h1>
      <a class="px-4 text-blue-600 cursor-pointer hover:opacity-80" href="/"
        >Create User</a
      >
      <div class="UserContainer flex flex-wrap gap-2 px-4 mt-2">
        <% if (users.length > 0) { %> <% users.forEach(function (user) { %>
        <div
          class="user bg-zinc-700 p-2 rounded-lg shadow-xl w-1/6 flex flex-col items-center justify-center"
        >
          <div class="h-76 w-full overflow-hidden">
            <img
              src="<%= user.image %>"
              alt=""
              class="w-full object-center rounded-lg mt-2"
            />
          </div>
          <h2 class="text-lg font-semibold tracking-tighter">
            Username: <%= user.username %>
          </h2>
          <p class="text-zinc-400 tracking-tighter">Email: <%= user.email %></p>
          <div class="flex justify-between w-full">
            <a href="/edit/<%=user._id%>" class="text-orange-200">Edit User</a>
            <a href="/delete/<%=user._id%>" class="text-red-400">Delete User</a>
          </div>
        </div>
        <% }); %> <% } else { %>
        <div><p class="text-gray-300">No users added yet</p></div>
        <% } %>
      </div>
    </div>
  </body>
</html>
